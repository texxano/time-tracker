import {
  StyleSheet,
} from 'react-native';
import colors from '../../../constants/Colors';



export const styles = StyleSheet.create({
  text: {
    color: colors.white,
    fontSize: 14,
  },
});
